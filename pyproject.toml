[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "scrinium"
version = "2.0.0"
description = "Scrinium — a research infrastructure for AI agents (hard fork of ScholarAIO)"
readme = {file = "README.md", content-type = "text/markdown"}
requires-python = ">=3.10"
license = "MIT"
authors = [
    {name = "Zimo Liao", email = "zimoliao@mail.ustc.edu.cn"},
]
keywords = ["academic", "literature", "literature-management", "research", "knowledge-base", "semantic-search", "claude", "agent-skills"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Text Processing :: Indexing",
]
dependencies = [
    "requests>=2.28",
    "pyyaml>=6.0",
    "defusedxml>=0.7",
    "beautifulsoup4>=4.12",
    "mineru-open-api>=0.2.2",
]

[project.urls]
Homepage = "https://github.com/wszqkzqk/scrinium"
Repository = "https://github.com/wszqkzqk/scrinium"
Issues = "https://github.com/wszqkzqk/scrinium/issues"

[project.optional-dependencies]
embed = [
    "sentence-transformers>=3.0",
    "numpy>=1.24",
    "faiss-cpu>=1.7",
]
topics = [
    "scrinium[embed]",
    "bertopic>=0.16",
    "pandas>=2.0",
]
pdf = [
    "pymupdf>=1.24",
]
import = [
    "endnote-utils>=1.0",
    "pyzotero>=1.5",
]
office = [
    "markitdown[docx,pptx,xlsx]>=0.1",
    "python-docx>=1.1",
    "python-pptx>=1.0",
    "openpyxl>=3.1",
]
# Only used by ad-hoc agent scripts from skills/draw; package code never imports these.
draw = [
    "cli-anything-inkscape>=1.0.0",
    "mermaid-py>=0.3",
]
full = [
    "scrinium[embed]",
    "scrinium[topics]",
    "scrinium[import]",
    "scrinium[pdf]",
    "scrinium[office]",
    "scrinium[draw]",
    "modelscope>=1.10",
    "curl-cffi>=0.5",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
    "mypy>=1.8",
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.24",
    "types-PyYAML>=6.0",
    "types-requests>=2.28",
    "pre-commit>=3.5",
]

[project.scripts]
scrinium = "scrinium.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["scrinium*"]
namespaces = false

# ---------- ruff ----------
[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
ignore = [
    "E501",   # line too long — handled by formatter
    "SIM108", # ternary operator — readability preference
    "B905",   # zip strict — Python 3.10 compat
    "UP007",  # X | Y union syntax — keep Optional[] for clarity
    "RUF001", # ambiguous Unicode char in string — Chinese text is intentional
    "RUF002", # ambiguous Unicode char in docstring — Chinese docstrings by design
    "RUF003", # ambiguous Unicode char in comment — Chinese comments by design
    "E741",   # ambiguous variable name — domain-specific short names (l, I, O)
]

[tool.ruff.lint.per-file-ignores]
# Optional-import heavy modules: BERTopic/numpy imported at runtime
"scrinium/topics.py" = ["F821"]
# Existing modules: suppress style nits that don't affect correctness
"scrinium/*.py" = ["SIM105", "SIM102", "SIM115", "E402", "F841", "RUF059", "B904"]
"scrinium/**/*.py" = ["SIM105", "SIM102", "SIM115", "E402", "F841", "RUF059", "B904"]

[tool.ruff.lint.isort]
known-first-party = ["scrinium"]

# ---------- mypy ----------
[tool.mypy]
python_version = "3.10"
warn_return_any = false
warn_unused_configs = true
ignore_missing_imports = true
disallow_untyped_defs = false
check_untyped_defs = false
disable_error_code = ["import-untyped"]

# Strict checking for new/well-typed modules
[[tool.mypy.overrides]]
module = ["tests.*"]
warn_return_any = true
check_untyped_defs = true

# Lenient for modules with heavy optional imports and dynamic typing
[[tool.mypy.overrides]]
module = [
    "scrinium.topics",
    "scrinium.vectors",
    "scrinium.cli",
    "scrinium.cli.*",

    "scrinium.explore",
    "scrinium.ingest.*",
]
ignore_errors = true

# ---------- pytest ----------
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
markers = [
    "slow: marks tests that need network or heavy computation",
]

# ---------- coverage ----------
[tool.coverage.run]
source = ["scrinium"]

[tool.coverage.report]
show_missing = true
skip_empty = true
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "if __name__",
]
