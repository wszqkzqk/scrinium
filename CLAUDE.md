# Scrinium - Project Instructions (Claude Code)

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This file is the Claude Code entry point; the shared project instructions live in `AGENTS.md` (the single source of truth) and are imported below via Claude Code's `@`-import mechanism. When Claude Code loads repository memory or plugin-bundled instructions, use this file as the primary instruction source.

@AGENTS.md
